import styled from "styled-components";

export const SearchStyled = styled.form`
  padding: .8em 0;
  position: relative;
  input {
    font-size: 1.2rem;
    padding:0 5px;
    height: 1.2em;
  }
  .search-icon{
    position: absolute;
    right: .2em;
    top: 1.1em;
    opacity: 0.7;
  }

`;